import { Widget, App, Utils, PopupWindow } from "imports"

const updateCommand = Utils.execAsync("bash -c 'paru -Syu'")

function UpdateChecker() {
    const updateLabel = 
    
})

}

export default () => PopupWindow({
    name: "updatenotifier",
    child: 
})